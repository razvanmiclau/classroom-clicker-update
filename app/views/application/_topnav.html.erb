<nav class="navbar navbar-default navbar-fixed">
    <div class="container-fluid">
        <div class="navbar-header">
            <h4 class="navbar-brand"><%= controller.controller_name.upcase %></h4>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-left">
              <% if controller.controller_name.upcase == 'QUESTIONS' && action_name == 'show' %>
                <li>
                  <%= link_to '#', class: 'toggle-fullscreen' do %>
                    <i class="glyphicon glyphicon-fullscreen"></i>
                  <% end %>
                </li>

                <li class="dropdown">
                  <%= link_to '#', {'data-toggle' =>  "dropdown", :class => 'dropdown-toggle'} do %>
                    <i class="glyphicon glyphicon-blackboard"></i>
                    <b class="caret"></b>
                    <ul class="dropdown-menu">
                      <li>
                        <%= link_to "#tab-questions", data: { toggle: "tab" } do %>
                          <span class="glyphicon glyphicon-th-list"></span>
                          Cards View
                        <% end %>
                      </li>
                      <% if @question.kind == 'Open Question' %>
                        <li>
                          <%= link_to "#tab-replies", data: { toggle: "tab" } do %>
                            <span class="glyphicon glyphicon-list-alt"></span>
                            Word Cloud
                          <% end %>
                        </li>
                      <% end %>
                      <% if @question.kind != 'Open Question' %>
                        <li>
                          <%= link_to "#tab-stats", data: { toggle: "tab" } do %>
                            <span class="glyphicon glyphicon-stats"></span>
                            Bar Chart
                          <% end %>
                        </li>
                        <li>
                          <%= link_to "#tab-pie", data: { toggle: "tab" } do %>
                            <span class="glyphicon glyphicon-pie"></span>
                            Pie Chart
                          <% end %>
                        </li>
                      <% end %>
                    </ul>
                  <% end %>
                </li>
              <% end %>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                  <%= link_to '#', {'data-toggle' =>  "dropdown", :class => 'dropdown-toggle'} do %>
                    <%= image_tag display_avatar(32), class: 'avatar' %>
                    <%= display_username %>
                    <b class="caret"></b>
                    <ul class="dropdown-menu">
                      <li><%= link_to 'Edit Accout', edit_user_registration_path %></li>
                      <li class="divider"></li>
                      <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
                    </ul>
                  <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>
